#include <stdio.h>
#include <stdlib.h>

int main() {
    int age = 40;
    double gpa = 3.6;
    char grade = 'A';
    char phrase[] = "Hello World";

    /*printf("%d, %d, %c, %s", age, gpa, grade, phrase);*/
    return 0;
}